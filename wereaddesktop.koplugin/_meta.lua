local _ = require("gettext")
return {
    fullname = _("微读"),
    version = require("wereaddesktop_version"),
    description = _([[微信读书桌面：启动（部分「启动时显示」模式除外）和退出书籍时显示微信读书书架，支持扫码登录、书城浏览、全书下载和阅读进度云同步。]]),
}
